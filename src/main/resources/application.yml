server:
  port: 8081

spring:
  application:
    name: member-service
  profiles:
    active: config
  config:
    import: "optional:configserver:http://10.0.16.245:8888/"

management:
  endpoints:
    web:
      exposure:
        include: "*"