server:
  port: 8081

spring:
  application:
    name: member-service
  profiles:
    active: config
  config:
    import: "optional:configserver:http://10.0.16.245:8888/"

  main:
    allow-bean-definition-overriding: true

  rabbitmq:
    host: 10.0.2.84
    port: 5672
    username: guest
    password: guest



management:
  endpoints:
    web:
      exposure:
        include: "*"